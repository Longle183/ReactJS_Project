export interface Product {
  id: number;
  name: string;
  price: number;
  image: string;
  // Add other fields as needed
}

export interface Discount {
  id: number;
  name: string;
  price: number;
  image: string;
  // Add other fields as needed
}